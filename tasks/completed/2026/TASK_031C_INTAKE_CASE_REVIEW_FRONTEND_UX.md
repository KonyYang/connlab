# TASK_031C_INTAKE_CASE_REVIEW_FRONTEND_UX

## Status

done

## Goal

Add the intake case review frontend surface for editable draft review before project confirmation.

## Scope

- Use `$impeccable` for UX decisions before UI edits.
- Show selected application form context.
- Show parsed draft fields as reviewable rows.
- Provide clear manual override and confirmation placement when backend endpoints are available.
- Keep formal project creation out of this task.

## Out Of Scope

- Word parsing implementation.
- Project confirmation backend.
- Precheck bridge.
- Outlook inbox auto-scan.
- Email sending.

## Required Implementation

- Read `PRODUCT.md`, `DESIGN.md`, and `DESIGN.json` if present.
- Follow the current light workbench visual direction.
- Add frontend build/static validation.
- Do not add backend endpoints unless the task is explicitly revised.

## Validation

- Run frontend build/static validation when UI files are touched.
