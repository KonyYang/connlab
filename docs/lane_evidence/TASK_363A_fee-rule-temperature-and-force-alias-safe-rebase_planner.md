# TASK_363A Planner Discovery Evidence

Date: 2026-07-18

Role: Planner

Status: `package_boundary_metadata_corrected / pending_reviewer_metadata_re_gate`

TASK_ID: `TASK_363A_FEE_RULE_TEMPERATURE_AND_FORCE_ALIAS_SAFE_REBASE`

Lane: `fee-rule-temperature-and-force-alias-safe-rebase`

## Current Phase / Why Allowed

Phase 11 controlled Matrix foundation. TASK_363A completed Developer implementation
and fixes plus earlier Reviewer/QA package-isolation re-gates. The immutable TASK_362A
r5 prior seed is accepted at `9e8dbe824334b7e41d55bbbd89d36a48a22edf7c`.
This Planner pass reconciles only the physical-line metadata correction identified by
the latest QA package-boundary gate; the production composition contract is unchanged.

## Evidence Read

- `AGENTS.md`, task board, Planner Discovery, parallel, orchestration, and role-thread
  protocols
- TASK_351 accepted force/default contract
- TASK_361L task/plan/evidence and actual V2 policy/rebase/persistence code
- TASK_362A task/evidence and active r5 seed/extension/compiler/loader facts
- current matcher, default-fill, reviewed extension defaults, draft service,
  dependency composition, and focused tests
- current git status and diff for seed/draft residuals
- the single logical `backend/api/dependencies.py` composition change, its
  constructor boundaries, diff fragments, and
  physical line-count delta

## Confirmed Findings

- Active r5 lacks exact alias `Temperature life`.
- Mechanical force source row 22 is `20/reading`, but broad aliases plus substring
  checks route ordinary mating/insertion/withdrawal/latch text to `50/sample`.
- Automotive source row 23 is already manual-required.
- Exact alias matching precedes generic token-subset matching; normalization already
  collapses case/punctuation/newlines.
- TASK_361L V2 has CAS/currentness infrastructure, but old rule-version lookup and
  provenance-aware Units/system-placeholder classification require an explicit
  transition policy.
- The accepted r5 files are currently mixed with external TASK_362A residuals and
  must not be edited or packaged by this Planner pass.

## Frozen Planner Decisions

- New immutable candidate seed/extension dated 2026-07-17; accepted r5 unchanged.
- Force and Automotive aliases are exact-only for this family.
- Positive aliases and negative precedence are as listed in the task.
- Only exact `Mating/Un-mating Force` retains the TASK_351 `50/sample` exception.
- Existing draft rebase requires unchanged non-rule lineage, bundled prior seed,
  exact row identity, reconstructed prior defaults, and field-level ownership.
- Pending/blank may refresh when proved system-owned; zero refresh requires positive
  system provenance; ambiguity blocks.
- Manual Unit Price, Units, Unit Type, Base Fee, Discount, Spend Time, and Notes are
  preserved; Testing Fee is derived.
- Load/Cancel is zero-write and reviewed save remains behind TASK_361L CAS/current-V2.

## Scope And Isolation

Future May Touch is limited to new seed artifacts, active manifest, matcher/alias
policy, reviewed force defaults, narrow V2 transition/rebase/persistence policy, the
single frozen `_build_fee_evaluation_pricing_draft_service` adapter-reuse/provider-
injection hunk, focused backend tests, and TASK_363A governance.

Frontend/API DTO/client, Fee visual design, formulas/prices outside confirmed aliases,
Point Profile/Measurement Plan/Matrix authority, schema, workbook/Required Forms
layout, generic outputs, parser/import, LTR/public drive, real DB/files, release/dist,
and external residuals remain locked.

## Definition Of Ready

Implementation and QA readiness are satisfied. The business mapping, exact/negative
alias matrix, retained exception, normalization/priority, seed versioning, safe-rebase
ownership policy, failure behavior, file boundaries, validation, and merge gates are
reviewable and testable. The accepted r5 baseline blocker is closed. Integrator
acceptance remains pending until the exact production-composition hunk passes
Reviewer, QA, and Integrator package-boundary re-gates.

## Validation Performed

- Confirmed accepted HEAD `9e8dbe824334b7e41d55bbbd89d36a48a22edf7c`
  supplies the immutable r5 prior seed identities.
- Confirmed `backend/api/dependencies.py` has one logical candidate change in
  `_build_fee_evaluation_pricing_draft_service`: `6` additions, `4` deletions, net
  `+2`. The QA-authoritative checked-out UTF-8 physical-line command convention,
  including blanks, reports HEAD `1958` and worktree `1960`.
- Confirmed the hunk only constructs one Measurement Plan adapter, reuses it for Fee
  defaults, and injects it as the pricing-draft `measurement_plan_provider`.
- Confirmed TASK_363A Integrator remains blocked and no package was staged or committed.
- No product/test/schema/API-client/real-file modification or execution occurred.

## Blockers

No product blocker. The corrected count metadata requires Reviewer package-boundary
metadata re-gate before QA and Integrator packaging resume.

## Next Legal Role

Reviewer package-boundary metadata re-gate.
