# TASK_GOVERNANCE_REGATE_EVIDENCE_REUSE_BASELINE_LEDGER_AND_VALIDATION_RUNNER Planner Evidence

Status: `planned_pending_user_approval`

Date: 2026-08-01

Role: permanent Planner

Planning base: `cdb96b4ed80143ba40d571615282f0ee95708a0f`

Hard dependency: Task A local Integrator acceptance; separate B User approval remains required.

## Authority Audit

- Primary was clean at revision base; production execution state is terminal/ownerless and Current
  Active Task is None.
- B has no branch, worktree, token, queue position, role dispatch, or implementation authority.
- The original umbrella is non-executable; Task A is the prerequisite approval package.
- No TASK_368E/product/test/evidence/retained lane was modified; its accepted history was read only.

## Sources Read

- Full rejection attachment and original umbrella task/plan/evidence.
- AGENTS sections 13-20, current board, Planner/Orchestrator skills, Discovery/WIP/parallel/
  orchestration/operations/execution/review protocols, and affected helper/test baselines.
- TASK_368E Developer, first and final Reviewer, QA, and Integrator evidence with exact ancestry,
  commands, Windows deselect incident, frontend runtime approach, and baseline debt proof.

## Discovery Classification

### Confirmed by User

- B is strictly serial after A and separately approved.
- Reuse is versioned, opt-in, per command, immutable, and fail-closed.
- Bounded authority/API/persistence changes rerun affected commands without automatically
  invalidating independent unchanged frontend commands.
- Schema/migration/public breaking/shared-owner/unprovable closure and unknowns force full re-gate.
- Deterministic argv runner, canonical Windows node IDs, verified read-only frontend runtime,
  isolated role-local shards, committed debt ledger, final-full QA, Integrator differential
  checks, TASK_368E replay, >=40% reduction, and measured medium pilot are required.

### Confirmed by Repository

- TASK_368E exact chain is base `e226bf1e...`, Developer `bb973483...`, blocker `68a33767...`, fix
  `1882c1b0...`, Developer fix `f924c33d...`, Reviewer pass `77fe429e...`, QA `c9a61bcb...`, merge
  `634279b7...`.
- Fix scope is authority + two bounded backend tests + Developer evidence; frontend paths are
  unchanged.
- Reviewer re-gate reran backend and unchanged frontend `61` tests/build; QA independently reran
  its full matrix including frontend `61`/build.
- QA's first Windows-backslash deselect failed to select the canonical slash node ID.
- The unchanged Matrix-session debt was safely attributed by identical Git blobs after a full base
  archive attempt timed out.
- A read-only existing frontend dependency tree was sufficient; lane install/lock mutation was not
  needed.

### Planner inference

- A seven-command-group replay (four affected backend/compile plus unchanged XLSX/catalog/external-
  read, frontend tests, and build) is the smallest faithful quantitative fixture; executing four
  and reusing three gives a `42.9%` reduction.
- A real-command replay in disposable workspaces is an adequate governance-only medium pilot and
  does not require a new product task or product mutation.
- Integrator validation should be differential only when QA evidence remains provably bound to the
  conflict-free merged tree; otherwise full validation is safer.

### Not yet confirmed

- A acceptance SHA, separate B approval, B worktree base, implementation/runtime hashes, and real
  pilot wall-clock/retries.

These are deliberate future gates. A acceptance and User approval block implementation but not
formal planning.

## Definition Of Ready

- Goal, serial dependency, per-command/global decisions, schemas/interfaces, exact TASK_368E replay,
  runner/ledger/isolation behavior, QA/Integrator states, paths, locks, rollback, validation,
  quantitative acceptance, lane identity, and role gates are explicit.
- There is no active owner conflict. B cannot be made approval-ready for execution until A is
  accepted; the task explicitly enforces that boundary.
- No further product or UX decision is required.

## Risk And Mitigation

- False reuse: exact command/dependency/environment/evidence hashes and global fail-closed reasons.
- Dependency omission: declared closure; unprovable closure forces full re-gate.
- Test selection error: canonical collection-validated node IDs.
- Runtime drift: lock/runtime/executable hashes and read-only/no-install policy.
- Hidden shard failure: unique outputs, immutable HEAD, complete deterministic aggregation.
- Baseline waiver abuse: committed owner/expiry/blob-bound ledger with no substring override.
- QA/Integrator duplication confusion: QA always full; Integrator differential only on exact proof.

## Stop Point

Keep B `planned_pending_user_approval`. Do not approve or implement it before A local acceptance
and separate User approval.
