# Frontend Smoke Test Results (Phase 9 & 10A)

**Date:** 2026-04-30  
**Tester:** User & AI Assistant  
**Status:** ⚠️ **Partial Pass** (Critical Blockers in Phase 10A)

---

## 1. Test Summary

| Category | Status | Notes |
| :--- | :--- | :--- |
| **Phase 9 (Operator Workflow)** | 🟡 **Mixed** | Core features passed (LTR, Lookup), but Folder record error blocks workflow. |
| **Phase 10A (Intake Entry)** | ⛔ **Blocked** | Case Review page is read-only; cannot input missing data to confirm project. |

---

## 2. Critical Blockers (Must Fix)

### 🔴 Issue 1: Case Review Data Missing (P9-7)
- **Checklist Item:** "Intake case review: confirm source context, reviewed fields, and confirmation blockers are visible."
- **Observation:** The "Intake Case Review" page shows the Source Context (Sender/Subject) but **fails to display the extracted form fields** (Product Name, Requester, etc.). The UI appears read-only and empty of form data.
- **Impact:** Operators cannot verify or correct data before project creation.

### 🔴 Issue 2: No Input Fields in Case Review (P10A-7/9)
- **Checklist Item:** "Case review: confirm source context, reviewed fields, operator notes, and confirmation blockers are visible."
- **Observation:** The review page correctly lists "Missing" fields (e.g., `Product Name`, `Requester`) but **provides no input boxes** for the operator to fill them in.
- **Impact:** The "Confirm into project" button remains permanently disabled. Operators are stuck in a "Missing Info" loop with no way to resolve it.

### 🔴 Issue 3: Folder Record Not Found (P9-8)
- **Checklist Item:** "Folder evidence: confirm evidence preview lists email, attachments..."
- **Observation:** Navigating to Step 4 (Project Folder) displays a workflow error: **"Project folder record not found for project..."**.
- **Impact:** Users cannot preview or generate folders. The workflow is interrupted before the final step.

---

## 3. Other Issues & Observations

###  Issue 4: Strict Candidate Detection Logic (P9-6)
- **Observation:** Files named `...test Request.docx` are scored low (45 pts) and treated as "Supporting Attachments" because they lack specific keywords like "Application Form" or "E-3718".
- **Impact:** Valid application forms with non-standard naming conventions are ignored by the auto-detection logic.
- **Workaround:** Files containing "E-3718" are correctly detected (Score 80+).

###  Passed Checks
- **P9-1 (Lookup):** Search works correctly.
- **P9-2 (Read-only Summary):** Sample and Testing summaries are visible and read-only in Project Workbench.
- **P9-3 (LTR Readiness):** LTR panel correctly lists missing fields ("Blocking fields") and review items.
- **P9-4 (LTR Preview):** UI indicates that preview does not write to the workbook.
- **P9-5 (LTR Commit):** Local commit button requires operator confirmation (checkbox).
- **P10A-1 to P10A-6 (Intake Entry):** Import, Manual Entry, and Package Detail all work correctly.
- **P10A-10 (Scope Guard):** UI is clean; no future-scope features (Matrix, AI, etc.) are exposed.

---

## 4. Recommended Next Steps

1.  **Add Edit Capability to Case Review:** Implement input fields in the "Reviewed fields" section of `IntakeCaseReviewPage.tsx` to allow operators to fill in missing data.
2.  **Debug Case Review Data:** Check if the backend API `GET /api/intake/cases/{id}` is returning the `draft` data correctly and if the frontend is mapping it to the form fields.
3.  **Fix Folder Initialization:** Investigate why the `ProjectFolderRecord` is not created when a project reaches the "LTR Registered" status.
4.  **Tune Candidate Detector:** Loosen the keyword matching in `ApplicationFormCandidateDetector` to include variations like "Test Request".

---
*Generated by Lingma AI Assistant*

## 5. TASK_066 Fix Status

**Date:** 2026-05-01

Resolved in `TASK_066_PHASE10A_SMOKE_BLOCKER_FIXES`:

- Case Review now renders editable reviewed fields.
- Missing `Product Name` and `Requester` can be saved as intake draft manual overrides before confirmation.
- Confirm into project remains blocked until backend review state reports required fields complete and the operator checks the confirmation box.
- Folder evidence preview no longer hard-fails when no project folder record exists; it returns a not-ready warning telling the operator to generate the project folder first.
- Evidence placement execution still requires an existing project folder record.

Validation:

- `py -m pytest -q` -> `244 passed`
- `npm run build` from `frontend\` -> passed
