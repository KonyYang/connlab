# 修改记录 - 2026-05-06

## 修改内容

### 1. 删除 New Project 页面右侧"Description of Requested Testing"表格的编辑按钮

**文件**: `frontend/src/features/precheck/PrecheckLowerPanels.tsx`

- 删除了 `onRequestedTestingRowEdit` prop 和相关参数
- 删除了编辑按钮（笔图标）的 JSX 代码
- 使"Description of Requested Testing"表格的操作按钮与"Test Sample Information"表格保持一致（只有复制和删除按钮）

**文件**: `frontend/src/features/new-project/NewProjectApplicationEditor.tsx`

- 删除了 `onRequestedTestingRowEdit` 回调函数

**文件**: `frontend/src/pages/IntakeCaseReviewPage.tsx`

- 删除了 `onRequestedTestingRowEdit` 回调函数

### 2. 统一两个表格的样式

**文件**: `frontend/src/intake-case-review.css`

- 将 `.requested-testing-edit-table` 的样式与 `.precheck-sample-table` 完全对齐
- 添加了 `table-layout: fixed`
- 统一了输入框的背景色、边框、圆角等样式
- 统一了聚焦状态和禁用状态的样式

### 3. 其他样式优化

**文件**: `frontend/src/intake-case-review.css`

- `textarea` 添加了 `overflow: hidden` 和 `resize: none` 来隐藏滚动条
- `precheck-additional-panel textarea` 添加了边框和圆角

## 测试方法

### 前端测试

1. **启动前端开发服务器**：
   ```powershell
   cd frontend
   npm run dev
   ```

2. **访问 New Project 页面**：
   - 在浏览器中打开 http://localhost:5173
   - 导航到"New Project"页面

3. **验证点**：
   - ✅ "Description of Requested Testing"表格不应该有编辑按钮（笔图标）
   - ✅ 只应该有复制和删除两个按钮
   - ✅ 表格的样式（背景色、边框、圆角等）应该与"Test Sample Information"表格一致
   - ✅ 输入框不应该显示垂直滚动条
   - ✅ 输入框聚焦时的样式应该一致
   - ✅ 输入框禁用时的样式应该一致

### 回归测试

1. **Test Sample Information 表格**：
   - 确认该表格功能正常（添加行、复制行、删除行）

2. **Additional Information 区域**：
   - 确认 textarea 可以正常编辑
   - 确认自动增长功能正常工作

## 已知限制

- 此修改仅影响前端 UI，不涉及后端逻辑
- 编辑功能已完全移除，如果需要恢复编辑功能，需要重新添加相关代码
