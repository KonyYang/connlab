# TASK_239 Plan

- Add minimal hook in group cell editor `onChange` to set `selectedGroupId` to current cell group.
- Keep existing row/group selection and context menu behavior unchanged.
- Add static test guard for this wiring.
