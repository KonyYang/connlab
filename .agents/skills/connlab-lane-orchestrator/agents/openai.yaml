interface:
  display_name: "ConnLab Lane Orchestrator"
  short_description: "执行 TASK 时自动隔离 worktree 并编排角色接力。"
  default_prompt: "执行指定 ConnLab TASK：自动扫描现有 lane/worktree，复用或创建独立 branch/worktree，检查 shared-path ownership，并在已授权范围内持续到本地 Integrator acceptance。"
