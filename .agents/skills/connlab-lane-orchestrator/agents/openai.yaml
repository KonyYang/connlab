interface:
  display_name: "ConnLab Lane Orchestrator"
  short_description: "按个人串行 V2 安全执行并收口 ConnLab 任务。"
  default_prompt: "使用 $connlab-lane-orchestrator 执行当前 ConnLab 任务，依据 board 与 Git 自动推进到人工验收。"
policy:
  allow_implicit_invocation: true
